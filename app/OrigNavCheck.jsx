// Scratch file left over from a lint comparison; safe to delete.
// Emptied because Meteor eagerly loads modules at the app root and the
// original contents' relative imports could not resolve from here.
export default null;
